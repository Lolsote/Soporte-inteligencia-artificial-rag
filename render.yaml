services:
  # Servicio Web para la aplicación Node.js
  - type: web
    name: soporteia-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: "3000"
      - key: AUTH_ALLOW_ANONYMOUS
        value: "true"
      - key: CHROMA_URL
        value: http://chromadb-service:8000
      - key: OLLAMA_BASE_URL
        value: http://localhost:11434 # Fallback offline integrado

  # Servicio Privado para la base de datos vectorial ChromaDB
  - type: pserv
    name: chromadb-service
    runtime: image
    image: docker.io/chromadb/chroma:latest
    envVars:
      - key: IS_PERSISTENT
        value: "TRUE"
      - key: PERSIST_DIRECTORY
        value: /chroma/chroma
      - key: ANONYMIZED_TELEMETRY
        value: "FALSE"
